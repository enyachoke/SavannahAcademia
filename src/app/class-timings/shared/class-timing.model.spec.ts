import {
  describe,
  ddescribe,
  expect,
  iit,
  it
} from '@angular/core/testing';
import {ClassTiming} from './class-timing.model';

describe('ClassTiming', () => {
  it('should create an instance', () => {
    expect(new ClassTiming()).toBeTruthy();
  });
});
